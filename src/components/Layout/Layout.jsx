import React, { Children } from 'react'
import AppBar from '../AppBar/AppBar'

const Layout = () => {
  return (
    <div>
        <AppBar />
        {/* {children} */}
    </div>
  )
}

export default Layout